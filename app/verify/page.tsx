"use client"
import { useEffect, useState } from "react"
import { SiteHeader } from "@/components/site-header"
export default function VerifyPage() { const [hash, setHash] = useState(""), [result, setResult] = useState<Record<string, unknown> | null>(null), [message, setMessage] = useState("Paste a Proofside canonical receipt hash."); useEffect(() => setHash(new URLSearchParams(window.location.search).get("hash") ?? ""), []); const verify = async () => { const response = await fetch(`/api/receipts/${encodeURIComponent(hash)}/verify`); const body = await response.json(); setResult(body); setMessage(response.ok ? (body.valid ? "Receipt content and available proof are valid." : "Verification failed.") : body.message) }; return <main><SiteHeader active="VERIFY" /><section className="hero compact"><p className="eyebrow">Public verifier</p><h1>Trust the receipt.<br />Check the proof.</h1></section><section className="verify-panel"><label>Canonical receipt hash<input value={hash} onChange={(event) => setHash(event.target.value)} placeholder="0x…" /></label><button className="trade-button" onClick={verify} disabled={!/^0x[0-9a-fA-F]{64}$/.test(hash)}>Verify receipt</button><p>{message}</p>{result && <pre>{JSON.stringify(result, null, 2)}</pre>}</section></main> }
