import { ForecastComposer } from "@/components/forecast-composer"
import { PredictionRoom } from "@/components/prediction-room"
import { ReceiptCard } from "@/components/receipt-card"
import { SiteHeader } from "@/components/site-header"
import { getForecastStore } from "@/lib/forecast-store"
export default function Home() { const receipts = getForecastStore().listRecent(3); return <main><SiteHeader active="FORECASTS" /><section className="hero"><p className="eyebrow">Credibility for humans and AI</p><h1>Anyone can sound confident.<br />DreamPulse proves it.</h1><p className="hero-copy">Publish a falsifiable forecast, anchor its receipt on Somnia, and let DreamDEX outcomes build a calibration record anyone can verify.</p></section><ForecastComposer />{receipts.length > 0 && <section className="receipt-section"><div className="section-title"><p className="eyebrow">Latest receipts</p><h2>Claims with a memory.</h2></div><div className="receipt-grid">{receipts.map((receipt) => <ReceiptCard key={receipt.id} receipt={receipt} />)}</div></section>}<section className="execution-section"><div className="section-title"><p className="eyebrow">Optional economic backing</p><h2>Back the thesis on DreamDEX.</h2><p>A forecast measures judgment. A position proves conviction. They remain separate and verifiable.</p></div><PredictionRoom /></section><footer><span>Settlement by <strong>DreamDEX</strong></span><span>Integrity proof by <strong>Somnia</strong></span><span>No custody. Every action is explicitly signed.</span></footer></main> }
