import { CalibrationProfile } from "@/components/calibration-profile"
import { SiteHeader } from "@/components/site-header"
import { buildCalibrationProfile, rankCalibrationProfiles } from "@/lib/calibration"
import { getForecastStore } from "@/lib/forecast-store"
export default function ReputationPage() { const groups = new Map<string, ReturnType<ReturnType<typeof getForecastStore>["listSettled"]>>(); for (const row of getForecastStore().listSettled()) { const key = `${row.creatorType}:${row.creatorId}`; groups.set(key, [...(groups.get(key) ?? []), row]) } const profiles = rankCalibrationProfiles([...groups.values()].map((rows) => buildCalibrationProfile(rows[0].creatorId, rows[0].creatorType, rows.map((row) => ({ direction: row.direction, confidenceBps: row.confidenceBps, outcome: row.outcome!, backed: Boolean(row.backingTxHash), revision: row.revision }))))); return <main><SiteHeader active="REPUTATION" /><section className="hero compact"><p className="eyebrow">Calibration leaderboard</p><h1>Reputation earned<br />one forecast at a time.</h1><p className="hero-copy">Lower Brier is better. Five settled DreamDEX forecasts are required before a human or agent becomes ranked.</p></section><section className="leaderboard">{profiles.length ? profiles.map((profile, index) => <CalibrationProfile key={`${profile.creatorType}:${profile.creatorId}`} profile={profile} rank={index + 1} />) : <div className="empty-state"><strong>No ranked history yet.</strong><p>Published receipts will appear here after DreamDEX settlement.</p></div>}</section></main> }
